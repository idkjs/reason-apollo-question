/* let component = ReasonReact.statelessComponent("ListItem");

   let make = (~item: ItemData.item, _children) => {
     ...component,
     render: _self =>
       <div>
         <h2> (ReasonReact.string(item.name)) </h2>
         (ReasonReact.string(string_of_int(item.age) ++ " years old."))
       </div>,
   }; */